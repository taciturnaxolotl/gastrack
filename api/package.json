{
	"name": "gastrack-api",
	"version": "0.1.0",
	"scripts": {
		"dev": "bun run --watch src/index.ts",
		"start": "bun run src/index.ts",
		"build": "bun build --compile --outfile dist/gastrack --production ./src/index.ts"
	},
	"dependencies": {},
	"devDependencies": {
		"@types/bun": "^1.3.6"
	},
	"private": true,
	"peerDependencies": {
		"typescript": "^5.9.3"
	}
}
